import { Observable } from 'rxjs';
import { of } from 'rxjs';
export class matPaginatorStub{
    // sendLang = of({});
	paginator(): Observable<any> {
		return of({});
	}
	// public sendMessage(message: any):Observable<any> {
    //     return of({});
    // }

    // clearMessages() {
    //     return of({});
    // }
}